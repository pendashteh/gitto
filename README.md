# gitto
cherries on top of git

```
Available tasks:
 - barebranch# @does rename HEAD branch on a bare repository.
 - date      # @arg date (e.g. 2 days ago) @does ammend the commit with the given date
 - diff      # 
 - git       # 
 - help      # Usage: gitto__ help [task]
 - log       # 
 - newfile   # @arg filenames @does adds the file path to index without the content.
 - search    # 
```
